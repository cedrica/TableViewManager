
package com.tableview.manager


import com.tableview.manager.TableScreen
import javafx.scene.layout.VBox
import javafx.scene.control.TableView

component Table controlledby TableScreen {
	
	VBox {
		TableView < ? > id table {
			static vgrow : "ALWAYS"
		}
	}
}
